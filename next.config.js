/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = {
  reactStrictMode: true,
  experimental: {
    appDir: true,
  },
};

// module.exports = nextConfig;
// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   reactStrictMode: true,
//   experimental: {
//     appDir: true, // เปิดใช้งาน app directory
//   },
// };

// module.exports = nextConfig;
